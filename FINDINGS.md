# Findings

Observations made during tasks that were deliberately **not** acted on (CLAUDE_CODE_INSTRUCTIONS §3 rule 1). Each needs a decision before anyone acts on it.

| # | Found during | Finding |
|---|---|---|
| F-001 | T-00 | `extruct` transitively installs `requests` and `urllib3` (via `mf2py` and `pyrdfa3`) plus `rdflib`, `beautifulsoup4`, `html5lib`, `lxml_html_clean`. NFR-2 says "no HTTP client" in the core install; the HTTP client arrives as a transitive dependency, not a direct one. Nothing in `src/semdiff/` will call it (hard rule 3), but the install footprint contradicts the letter of NFR-2. Options: accept and document; or call `extruct` syntax parsers selectively and vendor/replace `mf2py`/`pyrdfa3` usage later. |
| F-002 | T-00 | `git config user.email` is unset globally on this machine. Irrelevant until the first commit. |
| F-003 | T-02 | Naming inconsistency in ARCHITECTURE §Cross-cutting / T-02: `ParseError` and `InputTooLargeError` carry an `Error` suffix, `AlignmentBudgetExceeded` does not. Kept verbatim per D-010; a rename would be a public-API change. |
| F-004 | T-03 | The sdist includes `docs/`, `research/`, and `FINDINGS.md` (hatchling default). Harmless, but decide what the published sdist should contain before the first PyPI upload. |
| F-005 | T-13 | FR-5's illustrative Emotion pattern `css-[a-z0-9]{6,}` misses real 5-char hashes (`css-eu4da` on mui.com); the shipped rule uses `{5,}`. FR-5's CSS-modules example `[A-Za-z]+_[a-z0-9]{5,}` misses Docusaurus' 4-char mixed-case suffixes (`_O4bj`); the shipped rule requires a 4–8 char suffix with a digit or mixed case. Docs example regexes should be updated to match the shipped ruleset. |
| F-006 | T-14 | Stripping a hashed `id` leaves its references (`for=`, `aria-labelledby=`, `aria-controls=`, `href="#…"`) pointing at nothing. Harmless for diffing (both snapshots are treated identically) but T-18 `REPLACE_WITH_PLACEHOLDER` should apply the same placeholder to `id` and its referencing attributes so the link survives; T-15 tokens family should consider React `useId` values in those referencing attributes too. |
| F-007 | T-15 | `token.session_input` (entropy on hidden-input values) strips ant.design's 15 `<input type=hidden name=parameters>` values — lz-string-compressed demo source posted to CodeSandbox/CodePen, not tokens. Encoded payloads are indistinguishable from session tokens by shape. Deterministic and harmless for the noise-only gate (the visible code block still diffs), but a consumer relying on hidden payload changes would need to disable `token.session_input`. Documented limitation candidate for T-65. |
| F-008 | T-16 | docs.python.org (corpus fixture `docs-python-org-001`) rebuilds daily and prints `Last updated on Sep 17, 2026 (02:47 UTC).` — a human-readable date + `(HH:MM UTC)`, which is neither ISO-8601 nor relative phrasing, so FR-9 as written does not cover it. This pair will fail the T-21 gate on that footer alone unless FR-9 is extended with a conservative "human date + clock time (+ zone)" pattern. Decide at T-21 review, not before. |
| F-009 | T-17 | Full normalization (parse + all five families) takes 54–520 ms on the five T-04 captures (docs.python.org 110 KB: 519 ms, 978 whitespace applications) — 1.5–5× over the NFR-3 sub-100 ms target for the parse+normalize path. Likely cost: `Locator.for_node` builds a positional CSS path per application (a parent walk with sibling counting). Profile before T-19/T-21 and consider lazy or cheaper locators; not a T-17 correctness issue. |
