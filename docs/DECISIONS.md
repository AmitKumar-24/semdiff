# SemDiff — Decisions Log

Per `CLAUDE_CODE_INSTRUCTIONS.md` §9: every resolved design decision is recorded here with its date. Open questions are listed with the task they block; none may be resolved by guessing during implementation.

Precedence when documents disagree: `DOMAIN_MODEL.md` > `ARCHITECTURE.md` > `REQUIREMENTS.md` > `ROADMAP.md` > `PROJECT_BRIEF.md`. This log records the outcome; it does not replace the source documents, which should be amended when a decision changes them.

---

## Resolved

| ID | Date | Decision | Affects |
|---|---|---|---|
| D-001 | 2026-09-15 | **src layout.** Package lives at `src/semdiff/`; tests at `tests/`. Roadmap paths like `semdiff/normalize/rules/classes.py` are read as `src/semdiff/normalize/rules/classes.py`. | T-00, all code |
| D-002 | 2026-09-15 | **Git initialized, no commits.** Repository root is `semdiff/`. Commits happen only on explicit request. | T-00 |
| D-003 | 2026-09-15 | **Python ≥ 3.11.** Supersedes NFR-9 / T-00 "3.10+". `StrEnum` is used directly as written in `DOMAIN_MODEL.md`. | T-00, NFR-9 |
| D-004 | 2026-09-16 | **`Config` public surface.** `Config`, `ParserBackend`, `NormalizationConfig` are exported from `semdiff`. `config=None` in the public API means `Config()`. | T-01, T-19, T-43, T-61 |
| D-005 | 2026-09-16 | **Rule toggles are two frozensets** — `normalization.disabled_rules` / `normalization.enabled_rules` (overlap rejected). Chosen over `Mapping[str, bool]` for deep immutability and hashability. Serialized sorted. | T-01, T-12 (FR-11) |
| D-006 | 2026-09-16 | **Q-06, engine side.** `config_hash = sha256(canonical_json({ruleset_version: BUILTIN_RULESET_VERSION, config: <dump>}))`. The ruleset version is engine-owned, not a user field; T-12 takes ownership of the constant. Per-rule `version` on `NormalizationRule` is decided at T-12. | T-01, T-12 |
| D-007 | 2026-09-16 | **T-01 field scope.** Only `parser` (FR-2) and `normalization` toggles (FR-11). Deferred: `max_input_bytes` → T-10 (ceiling value to be supplied then); allow/deny selectors → T-32; filter-list pins → T-72; extraction prefs → T-38; thresholds/providers → T-90/T-92. Every field added later is hashed automatically (guarded by `test_every_field_is_covered_by_canonical_json`). | T-01 |
| D-008 | 2026-09-16 | **`errors[]` element shape** (`ErrorRecord`): `type: str`, `message: str`, `snapshot: "old" \| "new" \| null`, `detail: {str: str\|int\|null}` (keys serialized sorted). No `stage` field — `PipelineStage` arrives at T-56 and `type` already identifies the layer. Part of the `schema_version: "1.0"` contract. | T-02, T-57 |
| D-009 | 2026-09-16 | **Error type ids are snake_case:** `parse_error`, `input_too_large`, `alignment_budget_exceeded`. Stable forever, like rule ids. | T-02 |
| D-010 | 2026-09-16 | **Root exception is `SemDiffError`.** The three documented types are direct subclasses; no intermediate bases. `AlignmentBudgetExceeded` keeps its documented name (no `Error` suffix); see F-003. | T-02 |
| D-011 | 2026-09-16 | **`FrozenModel` lives in `src/semdiff/_models.py`** as the shared base for all config and output models. | T-01, T-02, T-57 |
| D-012 | 2026-09-16 | **Q-10(a) Label format = directory per pair.** `corpus/<category>/<id>/old.html`, `new.html`, `pair.json`. HTML stored as the bytes received, never reformatted. `pair.json` (stdlib JSON, no new dependency) carries at minimum: `label_version`, `category`, `url`, `captured_old`, `captured_new` (ISO dates), `content_type`, `expected` (noise-only: `{"change_types": []}`), optional `noise_families`, `notes`, and the mandatory provenance/license block from D-014. Entity-level expectations are added at T-44 after K is decided; `label_version` exists so T-04 fixtures never need rewriting. Exact field list is fixed at T-03. | T-03, T-04, T-44, T-73, T-77 |
| D-013 | 2026-09-16 | **Q-10(b) Capture strategy = same URL re-fetched across a multi-day gap (2c), first pass back-to-back (2b).** Raw HTTP GET, fixed User-Agent, no cookies, logged-out only, `robots.txt` respected; response bytes stored verbatim with `Content-Type`. No JS rendering (matches the MVP input model; rendered DOM is T-87). Tooling lives outside `src/semdiff/` (e.g. `scripts/`) and never runs in CI. Each pair passes a written noise-only verification rule before commit; pairs with real edits are parked for other categories. Site list, time gap, and the verification rule are still open (see Q-19..Q-21). | T-04 |
| D-014 | 2026-09-16 | **Q-10(c) Licensing = permissively-licensed sources by default, case-by-case otherwise (3d).** Every fixture's `pair.json` carries a mandatory provenance block: `license` (SPDX id or `proprietary`), `terms_url`, `attribution`, `basis` (`permissive` \| `case-by-case`). The corpus has its own `LICENSE`/`NOTICE` separate from the Apache-2.0 code, stating research/benchmark use and a takedown contact. Captured pages must not contain personal data. | T-03, T-04, T-101 |
| D-015 | 2026-09-16 | **`pair.json` v1 fields (T3-1), closed list:** `label_version: "1"`, `category`, `url`, `captured_old`, `captured_new`, `content_type`, `expected.change_types`, `noise_families` (optional), `notes` (optional), `license`, `terms_url`, `attribution`, `basis`. No other fields unless an existing document requires them. | T-03, T-04 |
| D-016 | 2026-09-16 | **Category vocabulary (T3-2), closed:** `noise_only`, `product`, `article`, `ad_injection`, `reorder`, `job`. New categories require a `label_version` change if the shape changes. | T-03 |
| D-017 | 2026-09-16 | **Corpus location (T3-3):** `corpus/` at the repository root, excluded from the wheel and sdist. | T-03 |
| D-018 | 2026-09-16 | **Fixture id scheme (T3-4):** `<host>-<NNN>` (host with dots as dashes, e.g. `mui-com-001`). Ids are stable once created and unique across the whole corpus. | T-03, T-04, T-22 |
| D-019 | 2026-09-16 | **Harness location (T3-5):** `tests/corpus/` only. No public `semdiff.corpus` module; the public API stays at `diff` / `normalize` / `extract` + CLI. | T-03 |
| D-020 | 2026-09-16 | **CI (T3-6):** GitHub Actions on `push` and `pull_request`; Python 3.11, 3.12, 3.13; runs pytest (incl. corpus suite) and `mypy --strict`. No network fetching in CI beyond dependency install. | T-03 |
| D-021 | 2026-09-16 | **Q-21 noise-only admission rule (T3-7), human-reviewed.** A pair is `noise_only` only when manual comparison confirms that, after accounting for the allowed noise families — dynamic CSS class values, dynamic/hash-like ids, token/session-like values, timestamp values — the meaningful visible text and DOM structure are unchanged. Any other meaningful text, element, attribute, or structural change disqualifies it. The rule is never phrased in terms of SemDiff's normalization rules. Review is recorded with the existing `noise_families` / `notes` fields only. | T-03, T-04, T-21 |
| D-022 | 2026-09-16 | **Q-19 site-list constraints.** No arbitrary commercial sites. Permissively licensed / public sites suited to CSS-in-JS or dynamically generated markup; 20 accepted pairs, different hosts preferred, several pages per host allowed if necessary. The concrete URL list with licensing basis must be presented and approved **before any fetch**. | T-04 |
| D-023 | 2026-09-16 | **Q-20 time gap.** First pass: back-to-back captures. Deploy-driven pass: multi-day gap, starting at ~7 days. Never assert a site changed without capturing and manually reviewing the pair. | T-04 |
| D-024 | 2026-09-16 | **Capture parameters (T4-1):** external script; fixed descriptive User-Agent; reasonable connect/read timeout; follow normal redirects; store raw response bytes exactly plus `Content-Type`; no cookies/authentication; `robots.txt` disallow is a hard stop; no capture/network code in `src/semdiff/`; no unnecessary complexity. | T-04 |
| D-025 | 2026-09-16 | **Rejected pairs (T4-2)** are not committed to `corpus/`; kept outside it only if needed for investigation. | T-04 |
| D-026 | 2026-09-16 | **Takedown contact (T4-3)** is never invented; it is requested before the corpus `NOTICE` is written. | T-04 |
| D-027 | 2026-09-16 | **Q-22: `captured_old` / `captured_new` are timezone-aware ISO-8601 datetimes** (e.g. `2026-09-16T10:42:07Z`). Date-only and naive values are rejected. | T-03, T-04 |
| D-028 | 2026-09-17 | **Q-06 resolved: `NormalizationRule.version: int` (default 1).** DOMAIN_MODEL §8 gains a `version` field. The ruleset fingerprint is `sha256(sorted [(id, version)])` computed by the registry; `Config.config_hash` reads it via `semdiff.normalize.registry.builtin_ruleset_version()` (local import). The old `BUILTIN_RULESET_VERSION` constant is gone. Human obligation: bump `version` when a rule's behaviour changes. | T-12, T-13+ |
| D-029 | 2026-09-17 | **Q-07 resolved (v0.1 scope): structured-data carriers are protected centrally by the engine.** TEXT-target rules skip text inside `<script>`, `<style>`, `<template>`; attribute rules skip `itemid itemref itemtype itemprop itemscope about resource typeof property vocab prefix`. Rules cannot opt out. `<time datetime>`, `<meta content>` and script *attributes* (e.g. `nonce`) remain in scope. Hash churn inside script text stays deferred (see Q-23 when raised). | T-12..T-17 |
| D-030 | 2026-09-17 | **Q-11 (partial): L1 lives in `src/semdiff/normalize/`** (`model.py`, `registry.py`; families under `normalize/rules/` from T-13). `Locator` lives in `src/semdiff/locator.py` (cross-cutting; reused by T-56). Remaining layer module names are fixed when their task arrives. | T-12, T-13, T-56 |
| D-031 | 2026-09-17 | **T-12 engine scope.** Targets: `attribute_value` (whitespace tokens), `attribute`, `node` (by tag), `text`. Actions: `strip`, `drop_node`; `replace_with_placeholder` → `NotImplementedError("T-18")`, `canonicalize` → `NotImplementedError("T-17")`. Registry orders by `(phase, id)`; a `canonical` rule must have a phase strictly greater than every non-canonical rule (rejected at registration otherwise). Unknown ids in `disabled_rules`/`enabled_rules` raise `ValueError`. `apply_rules` works on a fresh copy of the tree; the input `ParsedDoc` is never mutated. Locator `css` is a positional `:nth-child` path; `xpath` is `None` on selectolax. | T-12 |
| D-032 | 2026-09-18 | **Q-08 (T-17 part): the canonical serialization is Lexbor's `tree.html`** (selectolax). It already unifies void/self-closing forms, boolean attributes, tag/attribute-name case and entity spelling, so FR-8's "normalize self-closing forms" needs no rule and is not toggleable. Three toggleable `canonical.*` transforms run last (phase 90): `attr_order` (sort by lowercase name, re-inserted with the serializer's spelling so `viewBox` survives), `comments` (all comments, document level included, then adjacent text merged), `whitespace` (renderer-style: runs → one space; whitespace-only nodes dropped at block boundaries or first/last child, kept as one space between inline siblings; `<pre>`, `<textarea>`, script/style/template untouched). Doctype presence is not normalized. Verified idempotent on the five T-04 captures. Remaining Q-08 part — how `normalize()` serializes when `parser=lxml` — stays open for T-11/T-19. | T-17, T-19, T-21 |
| D-033 | 2026-09-24 | **T-19 public API signature.** `semdiff.normalize(html: bytes | str, config: Config | None = None, *, encoding: str | None = None) -> str`. `config` stays positional-or-keyword to match the signature written in ARCHITECTURE "Public API" (`parse()` keeps `config` keyword-only; the deviation is deliberate). `encoding` is forwarded to T-10 so undeclared non-UTF-8 bytes are usable; `url` is not exposed because it does not affect the output. The function lives in `src/semdiff/normalize/api.py` and is re-exported from `semdiff` and from `semdiff.normalize`; it shadows the subpackage attribute the same way `semdiff.parse` already does (see F-011). No report object is returned — rule provenance stays on `apply_rules()` until the T-20 CLI needs it. | T-19, T-20 |
| D-034 | 2026-09-24 | **Q-09 resolved: output encoding contract.** `normalize()` always returns `str`; bytes are decoded once at T-10 (declared `encoding` → BOM → `<meta charset>` → UTF-8, `errors="replace"`) and never re-encoded. Lexbor's serializer emits `&amp;`, `&lt;` and `&gt;` as entities and every other character literally (`&eacute;` → `é`, `&quot;` → `"`), so entity spelling is canonical by construction. An existing `<meta charset>` is preserved verbatim and never rewritten — it describes the *input* bytes and may disagree with the returned `str`; callers that re-encode own that. Output is valid Unicode text by construction (undecodable bytes became U+FFFD at parse). Correction after F-010: U+00A0 in element text is serialized as `&nbsp;` (raw-text elements keep it literal), which is likewise canonical — one spelling per document position. | T-19, T-20 |
| D-035 | 2026-09-25 | **F-010 fixed: `canonical.whitespace` collapses only HTML "ASCII whitespace"** — tab, LF, FF, CR, space (`[\t\n\f\r ]+`), not Python's Unicode-aware `\s`. U+00A0 (`&nbsp;`), U+2009, U+3000 and U+202F are content: they are never collapsed, never dropped as a whitespace-only node, and never treated as a collapsible boundary; ASCII runs *around* them still collapse. `canonical.whitespace.version` is bumped to `2` per D-028, which changes `Config.config_hash`. Re-verified idempotent on the five T-04 captures (mui.com: 453 → 445 applications; no other capture changed). | T-17, T-19, T-21 |

---

## Open — must be resolved before the blocking task

### Blocking Phase 0 / Phase 1

| ID | Question | Source of conflict | Blocks |
|---|---|---|---|
| Q-01 | **Which extras exist?** T-00 says `[content]/[ml]/[all]`; NFR-9 says `[content]/[all]`; research mentions `[browser]`. Proposed default for T-00: `[content]`, `[ml]`, `[all]` (per T-00, the most specific source). | ROADMAP T-00 vs REQUIREMENTS NFR-9 vs research | T-00 |
| Q-02 | **Doc filenames.** `CLAUDE_CODE_INSTRUCTIONS.md` references lowercase `docs/domain-model.md`, `architecture.md`, `requirements.md`, `roadmap.md`, `brief.md`, `decisions.md`. Actual files are `DOMAIN_MODEL.md`, `ARCHITECTURE.md`, `REQUIREMENTS.md`, `ROADMAP.md`, `PROJECT_BRIEF.md`, and this file is `DECISIONS.md`. Rename files or update references? | CLAUDE_CODE_INSTRUCTIONS §1, §9 | Docs hygiene only; no code task |
| Q-03 | **Operating instructions not auto-loaded.** They live at `docs/CLAUDE_CODE_INSTRUCTIONS.md`; Claude Code only auto-loads a repo-root `CLAUDE.md`. Move, copy, or add a root `CLAUDE.md` that points to it? | CLAUDE_CODE_INSTRUCTIONS location | Every session start |
| Q-04 | **`timings_ms` requires a clock.** Hard rule 3 forbids clock calls in the core diff path; `DOMAIN_MODEL.md` §10 puts `diagnostics.timings_ms` in the output. Options: (a) timings collected by the CLI / an outer wrapper and injected; (b) explicit exemption for a monotonic counter in diagnostics only. | CLAUDE §3 rule 3 vs DOMAIN_MODEL §10 | T-59 (diagnostics), T-62 (determinism test). *Not* T-01 — Config carries no timing setting. |
| Q-05 | **Decision K's gate is mislabelled.** ROADMAP and CLAUDE §9 say K (field-name namespace) blocks "Phase 1 schema", but Phase 1 (normalization) defines no entity field names. K first matters at T-34/T-36. Confirm Phase 1 may proceed without K. | ROADMAP "Decisions that gate the start" | T-10..T-22 (if K is held to block Phase 1) |
| Q-06 | **Resolved** by D-028. | — | — |
| Q-07 | **Resolved for v0.1** by D-029. | — | — |
| Q-08 | **Partly resolved** by D-032 (canonical serializer = Lexbor). Still open: serialization when `parser=lxml`, and whether `Locator.xpath = None` on selectolax is acceptable for MVP provenance. T-19 did not need either: the public API is selectolax-only and returns no locators. | ARCHITECTURE L0 vs FR-2, FR-23 | T-11, T-56 |
| Q-09 | **Resolved** by D-034. | — | — |
| Q-10 | **Resolved** by D-012 / D-013 / D-014. Remaining sub-decisions split out as Q-19, Q-20, Q-21. | — | — |
| Q-11 | **Package layout for future modules.** Roadmap implies `semdiff/normalize/rules/…`; the layer names (`parse`, `normalize`, `filter`, `extract`, `align`, `classify`, `output`) are not fixed anywhere. Fix them before T-10 so import paths appear correctly in provenance `extractor_id`s. | ARCHITECTURE layer names vs ROADMAP file paths | T-10, T-12 |
| Q-19 | **T-04 site list — URL list pending.** Constraints fixed by D-022; the concrete 20 URLs + licensing basis are proposed at T-04 planning and need approval before any fetch. | D-022 | T-04 |
| Q-20 | **Resolved** by D-023. | — | — |
| Q-21 | **Resolved** by D-021. | — | — |
| Q-22 | **Resolved** by D-027. | — | — |

### Blocking Phase 2

| ID | Question | Source of conflict | Blocks |
|---|---|---|---|
| Q-12 | **v0.1 / v0.2 scope: is there a text-diff baseline in the MVP?** PROJECT_BRIEF v0.1 and FR-22 include a "baseline diff" emitting `text_edit`, `content_added`, `content_removed`. ROADMAP Phase 1 says "No diffing at all" and Phase 2 has no task for text classifiers; text types first appear at T-83 (v0.3) / T-90 (v0.4). Either add a Phase 2 task or amend FR-22 and the brief. | PROJECT_BRIEF, FR-22 vs ROADMAP | T-49 (Change type set), T-57 (schema), T-65 (limitations) |
| Q-13 | **Change-object shape.** `DOMAIN_MODEL.md` §10 (highest precedence) is nested: `entity_type`, `entity_key: {scheme, value}`, `old_value: {amount, currency}`, `old_state` / `new_state`, `operation`, `confidence: {score, rule_id, basis}`, `detail`. ARCHITECTURE L6, PROJECT_BRIEF, and FR-23 show the older flat shape (`entity: "Product/Offer"`, `entity_key: "sku:ABC123"`, top-level `currency`, scalar `confidence`, `source`). The nested shape governs by precedence; the three lower-precedence documents need amending before `schema_version: "1.0"` is published. | DOMAIN_MODEL §10 vs ARCHITECTURE L6 / FR-23 / PROJECT_BRIEF | T-49, T-57 |
| Q-14 | **Change types in v0.2.** FR-22 lists five MVP types (`price_change`, `availability_change`, `content_added`, `content_removed`, `text_edit`). ROADMAP T-52..T-54 and DOMAIN_MODEL §5 ship `entity_added`, `entity_removed`, `extraction_gap`, `value_discordance` in v0.2 and omit the text types. Amend FR-22 to match. | FR-22 vs ROADMAP / DOMAIN_MODEL | T-49 |
| Q-15 | **Invariant 1 vs decision D.** If `VALUE_DISCORDANCE` (a single-snapshot finding) lives in `changes`, then "`changes == []` ⟺ no semantic change" and "Change = difference between a pair" both need rewording. DOMAIN_MODEL §12-D flags this itself. | DOMAIN_MODEL §5, §11 inv. 1, §12-D | T-54, T-60 |
| Q-16 | **Confidence table coverage.** DOMAIN_MODEL §6 has six rows. Missing combinations include Microdata/RDFa concordant and discordant, JSON-LD positional, heuristic concordant, and any `unknown`-state row for `EXTRACTION_GAP`. The table must be total over `ConfidenceBasis` before it is frozen. | DOMAIN_MODEL §6 | T-55 |
| Q-17 | **extruct input.** extruct takes an HTML string and parses with lxml internally; L3's input is the `FilteredDoc` tree. Re-serializing the filtered tree and re-parsing inside extruct is a second parse within the NFR-3 100 ms budget. Alternatives: run extruct on the original input and map provenance across stages; or pass an lxml tree when the lxml backend is active. | ARCHITECTURE L3 vs FR-2, NFR-3 | T-38 |
| Q-18 | **Invariant 1 vs error results.** Invariant 1 says `changes == []` ⟺ no semantic change, *and* that an error "populates `errors`". An error result presumably also has `changes: []`, which contradicts the ⟺ literally. Needs wording — e.g. the invariant holds only when `errors == []`, or an error result omits/nulls `changes`. Not resolved by T-02, which only defines the exception classes and `ErrorRecord`. | DOMAIN_MODEL §11 inv. 1 vs §10 | T-57, T-60, T-61 |

### Deferred design ambiguities (DOMAIN_MODEL §12)

A–L remain open. Per `CLAUDE_CODE_INSTRUCTIONS.md` §9, **K, A, B, C** must be settled before the tasks they gate (T-34, T-36, T-48, T-49; see Q-05 on K's actual gate). D–J and L are decided when their phase arrives. Each answer is appended to the Resolved table above with its date.
